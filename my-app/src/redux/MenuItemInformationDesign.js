let initialState = {
    MenuItemInformationDesign: [
        {nameMenu: "Оформление соц сетей", menuLink: "/socialweb"},
        {nameMenu: "Фирменный дизайн группы", menuLink: "/design"}
    ]
};

const MenuItemInformationDesignReducer = (state = initialState) => {
    return state;
};


export default MenuItemInformationDesignReducer;