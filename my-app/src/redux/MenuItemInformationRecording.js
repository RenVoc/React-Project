let initialState = {
    MenuItemInformationRecording: [
        {nameMenu: "Сессионная запись гитар и вокала", menuLink: "/recording"},
        {nameMenu: "Драм-треки, биты и тексты", menuLink: "/beats"}
    ]
};

const MenuItemInformationRecordingReducer = (state = initialState, action) => {
    return state;
};


export default MenuItemInformationRecordingReducer;